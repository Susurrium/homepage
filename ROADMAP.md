# 项目路线图

## 当前状态

站点技术骨架与第一轮阅读体验优化已完成：Astro 内容集合、Markdown/MDX、文章与项目详情、分页、标签、年度归档、全文 RSS、Sitemap、Pagefind Component UI、主题切换、动态签名、GitHub Pages 子路径适配和 CI 质量门禁均已接通。

文章页现已具备阅读进度、响应式目录、代码复制、标题链接复制、图片灯箱、更新时间、Article JSON-LD 和题图优化。自定义 404、Web App Manifest、分享元数据以及桌面/移动端 Playwright 与 Axe 检查也已纳入发布基线。

## 发布前确认

- 将当前工作分支通过 merge、rebase 或 Pull Request 整合到远端 `master`。
- 在仓库 Settings → Pages 中将发布来源设为 GitHub Actions。
- 按需在 `src/data/site.ts` 补充邮箱、所在地和教育经历；留空时页面不会展示虚构信息。
- 用真实经历和项目持续替换或扩充现有内容集合。

## 需要产品决策的后续能力

这些能力不应在缺少账号、隐私和内容策略时默认启用：

- 评论：优先考虑 Giscus。启用前需要开启 GitHub Discussions，并确定仓库、分类及公开讨论规则。
- 访问统计：选择无 Cookie 或隐私友好的统计服务，并确认站点域名、数据保留与隐私说明。
- 内容图片：题图已进入 Astro 图片管线；真实正文和项目图片补齐后，再按实际尺寸设计响应式 `srcset` 与现代格式策略。
- 多语言：先确定 `/en/` 等 URL 结构、默认语言、翻译范围及未翻译内容的回退方式。

## 持续维护

- 每次提交前运行 `npm test` 和 `npm audit`。
- 新增文章时使用稳定文件名，补齐描述、日期、分类和标签。
- 依赖大版本升级后复核构建、搜索、深浅主题和移动端布局。
